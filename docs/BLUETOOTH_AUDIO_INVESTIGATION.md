# Звіт та стан налаштування Bluetooth-аудіо (JX-BT & JBL)

**Дата:** 17 вересня 2026 р.  
**Статус:** Призупинено для продовження пізніше (`paused_to_continue_later`).  
**Ціль:** Стабільна трансляція інтернет-радіо та музики на колонку `JX-BT` (1-й поверх) через веб-дашборд та голосові/текстові команди.

---

## 1. Апаратне та програмне оточення

| Компонент | Значення / Модель | Примітки |
| :--- | :--- | :--- |
| **Хост (сервер)** | Lenovo ThinkPad Edge E530c (Type 33664LG) | Linux Mint 22.3, ядро `Linux 7.0.0-30-generic` |
| **Wi-Fi адаптер** | Broadcom BCM4313 802.11bgn [14e4:4727] | Спільна 2.4 ГГц антена з Bluetooth |
| **Bluetooth адаптер** | Broadcom Corp. BCM20702A0 (`0a5c:21f4`, `bcdDevice 1.12`) | Працює через шину USB (`usb 2-1.3`) |
| **Цільова колонка** | `JX-BT` (`41:42:62:69:51:9B`) | Bluetooth аудіо-приймач / колонка (1-й поверх) |
| **Друга колонка** | `Юрій: JBL Clip 5` (`F8:5C:7E:EE:7D:CC`) | Робоча колонка в гаражі |
| **Аудіосервер** | PipeWire 1.0.5 + WirePlumber 0.4.17 | Сесія користувача `yurko` |

---

## 2. Хронологія та виконані роботи

### 2.1. Пошук та реєстрація пристрою JX-BT
* Проведено сканування ефіру через `bluetoothctl`. Знайдено пристрій `41:42:62:69:51:9B` з назвою `JX-BT`.
* Пристрій додано в системну пам'ять:
  * [`devices/bluetooth_devices.json`](file:///home/yurko/AI/SmartGarage/devices/bluetooth_devices.json)
  * [`memory/memory.json`](file:///home/yurko/AI/SmartGarage/memory/memory.json)

### 2.2. Інтеграція Інтернет-Радіо та виправлення маршрутизації
* Додано сервіс `RadioService` ([server/services/radio_service.py](file:///home/yurko/AI/SmartGarage/server/services/radio_service.py)) з каталогом українських радіостанцій (Kiss FM, Hit FM, Radio ROKS тощо) через Radio Browser API.
* **Багфікс маршрутизатора:** у [server/commands/processor.py](file:///home/yurko/AI/SmartGarage/server/commands/processor.py) команди виду *«включи радіо kiss fm»* раніше помилково перехоплювалися парсером YouTube-відео (через ключове слово «включи»). Додано виключення для «радіо»/«radio», тепер запити гарантовано запускають стрім онлайн-радіо.
* **Оновлення дашборду:** у [server/templates/dashboard.html](file:///home/yurko/AI/SmartGarage/server/templates/dashboard.html) та [server/static/js/dashboard.js](file:///home/yurko/AI/SmartGarage/server/static/js/dashboard.js) додано чіпи радіостанцій, підтримку динамічної назви підключеної колонки та повзунок гучності.

---

## 3. Виявлені проблеми та технічні причини

### Проблема 1: Звук грав «ривками», заїкався
1. **Шумодав (Noise Gate):** системна гучність у PipeWire була наднизькою (13–20%, -52 dB). Апаратний цифровий шумодав на чіпі приймача глушив звук на тихих ділянках і різко вмикав його на басах.  
   *Виправлено:* зафіксовано базову гучність на рівні **75–80%**.
2. **Невідповідна буферизація GStreamer:** запуск `playbin` з `buffer-duration=3000000000` на живому онлайн-потоці змушував GStreamer постійно прискорювати або сповільнювати відтворення (ефект «гумового» або «завиваючого» звуку).  
   *Виправлено:* прибрано штучний буфер часу, залишено стандартний потоковий потік.
3. **Конфлікт проміжних аудіосерверів:** плеєр використовував `pulsesink`, що викликало розсинхронізацію тактового генератора між PipeWire та PulseAudio-емуляцією.  
   *Виправлено:* у [server/devices/bluetooth_speaker.py](file:///home/yurko/AI/SmartGarage/server/devices/bluetooth_speaker.py) плеєр переведено на нативний `pipewiresink target-object=...`.

### Проблема 2: Падіння в телефонний режим 8 кГц (HSP/HFP CVSD)
* При спробі відтворення WirePlumber автоматично перемикав профіль Bluetooth-карти у `headset-head-unit-cvsd` (монофонічний телефонний дзвінок низької якості).
* *Виправлено:* створено файл конфігурації `~/.config/wireplumber/bluetooth.lua.d/51-bluez-a2dp-only.lua`, який повністю вимикає `hfp_hf/hsp_hs` та блокує з'єднання виключно у високоякісному стерео-режимі **A2DP**.

### Проблема 3: Звук «завився і зник» (Stalled ACL / TX Timeout)
* Для вирішення повідомлення ядра `BCM: firmware Patch file not found` було завантажено патч `BCM20702A1-0a5c-21f4.hcd`.
* **Причина збою:** Апаратний чіп у ноутбуці — це ревізія **BCM20702A0** (`bcdDevice 1.12`), тоді як патч був для новішої ревізії **A1**.
* Через несумісність ревізій у ядрі виникли помилки:
  ```text
  [ 8651.908718] Bluetooth: hci0: link tx timeout
  [ 8651.908737] Bluetooth: hci0: killing stalled connection 41:42:62:69:51:9b
  ```
  Чіп зависав після відправки ~100 пакетів і скидав лінк.

---

## 4. Скрипти керування, створені в проєкті

1. **[`scripts/install_bt_firmware.sh`](file:///home/yurko/AI/SmartGarage/scripts/install_bt_firmware.sh)** — скрипт завантаження та копіювання мікропрограми Broadcom у `/lib/firmware/brcm/`.  
   * **⚠️ ВАЖЛИВЕ ЗАСТЕРЕЖЕННЯ:** Скрипт призначений **виключно** для адаптерів ревізії **BCM20702 A1**. На поточному сервері (ThinkPad E530c) встановлено ревізію **BCM20702 A0** (`bcdDevice 1.12`), на якій цей патч спричиняє критичне зависання Bluetooth-лінка (`link tx timeout` та `killing stalled connection`). З цієї причини скрипт містить обов'язкове інтерактивне підтвердження (або прапорець `--force`) і його **НЕ слід** застосовувати на цій апаратній конфігурації.
2. **[`scripts/remove_bt_firmware.sh`](file:///home/yurko/AI/SmartGarage/scripts/remove_bt_firmware.sh)** — скрипт видалення несумісного файлу прошивки та повернення до стабільного заводського ROM ядра:
   ```bash
   cd /home/yurko/AI/SmartGarage && ./scripts/remove_bt_firmware.sh
   ```

---

## 5. План дій на наступний сеанс (Чек-лист з фактичними статусами)

- [x] **Крок 1. Відкат прошивки (Виконано):**  
  Несумісний файл `/lib/firmware/brcm/BCM20702A1-0a5c-21f4.hcd` видалено з сервера, стек `btusb` працює на заводському ROM (build 0000). Відсутність файлу в системному каталозі перевірена та підтверджена.
- [ ] **Крок 2. Фізична перевірка приймача JX-BT (Очікує підтвердження користувачем):**  
  * Перевірити живлення JX-BT (чи світиться/блимає індикатор Bluetooth).
  * Перевірити підключення AUX-кабелю до входу підсилювача/колонок (чи немає обриву/поганого контакту).
  * Перевірити механічний регулятор гучності на колонці чи підсилювачі (чи не викручений на нуль).
- [ ] **Крок 3. Тест локальним звуком (Готовий до тестування):**  
  Виключити вплив інтернет-радіо та перевірити пряме відтворення короткого тестового сигналу PipeWire безпосередньо у Bluetooth-приймач:
  ```bash
  # Знайти назву Bluetooth sink (зазвичай bluez_output.41_42_62_69_51_9B.1):
  wpctl status | grep -i "JX-BT"
  # Відтворити тестовий звук прямо на аудіо-вихід JX-BT:
  pw-play --target bluez_output.41_42_62_69_51_9B.1 /usr/share/sounds/freedesktop/stereo/complete.oga
  ```
- [x] **Крок 4. Захист та запуск інтернет-радіо через API (Захищено та покрито тестами):**  
  Ендпоінт `/api/radio/play` та метод `BluetoothSpeaker.play_stream` захищено allow-list валідацією URL схем (дозволено виключно `http://` та `https://`, спроби передати локальні шляхи чи `file://` відхиляються з HTTP 400). Покритий unit-тестами. Запуск радіо після встановлення BT-з'єднання:
  ```bash
  curl -s -X POST http://localhost:5000/api/radio/play -H "Content-Type: application/json" -d '{"station": "kiss_fm"}'
  ```
- [ ] **Крок 5. Порівняльний тест A/B (Рекомендований апаратний fallback):**  
  Якщо JX-BT підключено, лічильник пакетів зростає (`hciconfig hci0 | grep TX`), але аудіовихід мовчить — підключити другу колонку **JBL Clip 5** (`F8:5C:7E:EE:7D:CC`), щоб точно ізолювати: проблема в аналоговому тракті/підсилювачі JX-BT чи в Bluetooth-модулі ноутбука.

